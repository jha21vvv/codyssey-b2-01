# 📖 가계부 콘솔 서비스 구현 과제: 문제-답 완벽 스터디 가이드

---

## 📑 목차
1. [아키텍처 구조화 및 모듈 분리](#1-아키텍처-구조화-및-모듈-분리)
2. [데이터 모델(Data Model) 및 무결성 검증](#2-데이터-모델data-model-및-무결성-검증)
3. [파일 영구 저장 및 원자적 쓰기(Atomic Write)](#3-파일-영구-저장-및-원자적-쓰기atomic-write)
4. [제너레이터 기반 스트리밍(Streaming I/O)](#4-제너레이터-기반-스트리밍streaming-io)
5. [거래 추가 (Add) 대화형 인터페이스](#5-거래-추가-add-대화형-인터페이스)
6. [거래 목록 조회 (List) 및 정렬](#6-거래-목록-조회-list-및-정렬)
7. [거래 다중 조건 검색 (Search)](#7-거래-다중-조건-검색-search)
8. [거래 수정(Update) 및 삭제(Delete)](#8-거래-수정update-및-삭제delete)
9. [카테고리 관리 (Category) 및 데이터 보호](#9-카테고리-관리-category-및-데이터-보호)
10. [월별 요약(Summary) 및 예산 경고(Budget)](#10-월별-요약summary-및-예산-경고budget)
11. [CSV 가져오기(Import) 및 내보내기(Export)](#11-csv-가져오기import-및-내보내기export)
12. [공통 관심사 데코레이터 및 예외 제어(Decorator & Exit Code)](#12-공통-관심사-데코레이터-및-예외-제어decorator--exit-code)

---

## 1. 아키텍처 구조화 및 모듈 분리

### 📌 [과제 명세서의 문제 요구사항]
- 한 파일에 모든 코드를 몰아넣지 말고 최소 3개 이상의 모듈로 분리할 것.
- (권장) CLI(화면) / Service(비즈니스 로직) / Repository(파일 I/O) / Model(데이터 구조)로 책임을 나눌 것.
- `python -m budget_app <command>` 형태로 패키지 실행이 가능해야 함.

### 💡 [3개월 차를 위한 쉬운 비유와 용어 해설: "뭘 하라는 건가?"]
- **비유**: 백종원의 식당 주방과 홀 분리
  - 한 사람이 요리도 하고, 서빙도 하고, 계산도 하고, 식자재 창고 정리까지 다 하면 식당이 엉망진창이 됩니다.
  - **CLI (홀 서빙/키오스크)**: 손님에게 메뉴판을 보여주고 주문을 받습니다.
  - **Service (주방장/회계사)**: 실제로 요리를 만들고, 원가와 예산을 똑똑하게 계산합니다.
  - **Repository (창고지기)**: 냉장고와 장부 파일에 식자재와 돈을 안전하게 넣고 뺍니다.
  - **Model (공식 레시피/주문서 양식)**: 주문서 종이 규격을 통일합니다.
- **실질적으로 뭘 하라는 것인가?**: 코드가 길어지면 찾기 힘드니, 파일 역할을 4개로 쪼개서 `cli.py`, `services.py`, `repositories.py`, `models.py`로 나눠 담으라는 뜻입니다.

### 💻 [답안 구현 위치 및 코드]
- **구현 파일**: `budget_app/__init__.py`, `budget_app/__main__.py`
```python
# [__init__.py 전체 코드]
# 왜 만들었는가: 
# 1. 파이썬이 budget_app 폴더를 열었을 때 이 파일을 발견함으로써,
#    "아! 이 폴더는 사진이나 잡동사니 폴더가 아니라, 진짜 가계부 부품들이 들어있는 '공식 프로그램 상자(패키지)'구나!"라고 확인하고 공식 인정하게 만듭니다.
# 2. 프로그램 상자 겉면에 버전 스티커(__version__ = "1.0.0")를 딱 붙여둡니다.
__version__ = "1.0.0"
```
```python
# [__main__.py 전체 코드]
from .cli import run_cli

if __name__ == "__main__":
    run_cli()
```
- **계층 분리 파일 구성**:
  - `__init__.py`: "이 폴더는 진짜 가계부 프로그램 상자다!" 공식 인정 및 버전 명찰 부착
  - `__main__.py`: 터미널 실행 시 화면을 켜주는 자동차 시동 키
  - `models.py`: 데이터 구조(Transaction, Budget, Category)
  - `repositories.py`: 파일 읽기/쓰기(FileRepository)
  - `services.py`: 비즈니스 로직 및 계산(BudgetService)
  - `cli.py`: 콘솔 입출력 및 메뉴 제어(run_cli)
  - `decorators.py`: 공통 에러 차단

### 🔍 [어떤 점에서 요구사항이 구현되었는가?]
1. `__init__.py`가 존재함으로써 파이썬 인터프리터가 `budget_app` 디렉터리를 단순 폴더가 아닌 정식 패키지 모듈로 인식하며 버전 정보를 메모리에 등록합니다.
2. `__main__.py`를 만들어 터미널에서 `python -m budget_app` 명령어로 즉시 구동됩니다.
3. 5개의 전용 모듈로 완벽하게 책임을 분리하여, 화면 코드가 바뀌어도 저장소 코드에는 전혀 영향을 주지 않는 유지보수 가능한 Clean Architecture 구조를 완성했습니다.

---

## 2. 데이터 모델(Data Model) 및 무결성 검증

### 📌 [과제 명세서의 문제 요구사항]
- 거래 내역(`Transaction`)은 필수 필드(`id`, `type`, `date`, `amount`, `category`, `memo`, `tags`)를 반드시 포함할 것.
- `dataclass` 또는 그에 준하는 구조를 사용하고, 최소 2개 이상의 클래스를 정의할 것.
- 음수 금액, 잘못된 날짜 형식, 비어있는 카테고리 등을 자체 검증할 것.

### 💡 [3개월 차를 위한 쉬운 비유와 용어 해설: "뭘 하라는 건가?"]
- **비유**: 은행의 공식 영수증 서식과 입구 검사관
  - 이면지에 "철수한테 500원 줌"이라고 삐뚤빼뚤 낙서해 오면 은행원이 받아주지 않습니다.
  - 반드시 공식 서식(`@dataclass`)에 적어야 하고, 서식을 내자마자 검사관(`__post_init__`)이 돋보기를 들고 "날짜가 2월 30일이라고? 금액이 마이너스 5만 원이라고? 빠꾸!" 하고 엉터리 데이터를 입구 컷 하라는 뜻입니다.

### 💻 [답안 구현 위치 및 코드]
- **구현 파일**: `budget_app/models.py`
```python
@dataclass
class Transaction:
    id: str
    type: Literal["income", "expense"]
    date: str
    amount: int
    category: str
    memo: str = ""
    tags: list[str] = field(default_factory=list)

    def __post_init__(self) -> None:
        if self.type not in ("income", "expense"):
            raise ValueError(f"유효하지 않은 거래 타입입니다: '{self.type}'. 'income' 또는 'expense'여야 합니다.")

        try:
            datetime.strptime(self.date, "%Y-%m-%d")
        except ValueError:
            raise ValueError(f"유효하지 않은 날짜 형식입니다: '{self.date}'. 'YYYY-MM-DD' 형식이어야 합니다.")

        if not isinstance(self.amount, int) or self.amount <= 0:
            raise ValueError(f"금액은 0보다 큰 양수 정수여야 합니다: {self.amount}")

        if not self.category or not self.category.strip():
            raise ValueError("카테고리는 비어 있을 수 없습니다.")
```

---

## 3. 파일 영구 저장 및 원자적 쓰기(Atomic Write)

### 📌 [과제 명세서의 문제 요구사항]
- 표준 라이브러리만 사용하여 3개 이상의 분리 파일(`transactions`, `categories`, `budgets`)에 영구 저장할 것.
- 기본 경로는 `./data`이고 변경 가능해야 함. 파일 부재 시 자동 생성 초기화할 것.
- 수정/삭제 시 데이터 손상이 없도록 "원자적 교체(Atomic replace)"를 적용할 것.

### 💡 [3개월 차를 위한 쉬운 비유와 용어 해설: "뭘 하라는 건가?"]
- **비유**: 연습장에 먼저 쓰고 진짜 장부와 순식간에 바꿔치기하는 마술
  - 컴퓨터는 가끔 정전이 나거나 배터리가 나갈 수 있습니다.
  - 원본 장부 파일을 열어서 글씨를 쓰다가 중간에 컴퓨터가 꺼져버리면, 원본 장부 전체가 0바이트(빈 깡통)로 깨져서 가계부가 날아갑니다.
  - 그래서 임시 연습장 파일(`tempfile`)에 완벽하게 글씨를 다 적고, 하드디스크에 물리적으로 기록된 것까지 확인(`os.fsync`)한 다음, **0.001초 만에 원본 파일 이름으로 싹 맞바꾸는(os.replace)** 기술을 쓰라는 것입니다.

### 💻 [답안 구현 위치 및 코드]
- **구현 파일**: `budget_app/repositories.py`
```python
    def overwrite_transactions(self, transactions: List[Transaction]) -> None:
        dir_name = os.path.dirname(self.tx_file)
        with tempfile.NamedTemporaryFile("w", dir=dir_name, delete=False, encoding="utf-8") as tf:
            temp_path = tf.name
            for tx in transactions:
                line = json.dumps(tx.to_dict(), ensure_ascii=False)
                tf.write(line + "\n")
            tf.flush()
            os.fsync(tf.fileno())

        os.replace(temp_path, self.tx_file)
```

---

## 4. 제너레이터 기반 스트리밍(Streaming I/O)

### 📌 [과제 명세서의 문제 요구사항]
- 파일 전체를 메모리에 한 번에 `read()`로 올리지 말고, `yield` 기반의 제너레이터(Generator)로 한 줄씩 스트리밍 처리할 것.

### 💡 [3개월 차를 위한 쉬운 비유와 용어 해설: "뭘 하라는 건가?"]
- **비유**: 뷔페 접시 vs 회전초밥
  - 10만 줄짜리 장부 파일을 한꺼번에 `lines = f.readlines()`로 읽으면, 뷔페 접시에 음식 100그릇을 한 번에 쌓아 올리다 접시가 깨지는 것처럼 컴퓨터 메모리(RAM)가 터져버립니다.
  - `yield`를 쓰면 회전초밥 레일처럼 **"손님이 원할 때 딱 한 접시(한 줄)씩만 눈앞으로 지나가게"** 만들어, 10기가짜리 대용량 파일도 컴퓨터가 버벅대지 않고 가볍게 읽을 수 있게 하라는 뜻입니다.

### 💻 [답안 구현 위치 및 코드]
- **구현 파일**: `budget_app/repositories.py`
```python
    def stream_transactions(self) -> Generator[Transaction, None, None]:
        if not os.path.exists(self.tx_file):
            return

        with open(self.tx_file, "r", encoding="utf-8") as f:
            for line in f:
                line_str = line.strip()
                if not line_str:
                    continue
                data = json.loads(line_str)
                yield Transaction.from_dict(data)
```

---

## 5. 거래 추가 (Add) 대화형 인터페이스

### 📌 [과제 명세서의 문제 요구사항]
- `add` 실행 시 날짜, 타입, 카테고리, 금액, 메모, 태그를 `input()`으로 순차 입력받을 것.
- 카테고리는 등록된 목록에 있어야 하며, 저장 완료 시 고유 id를 출력할 것.

### 💡 [3개월 차를 위한 쉬운 비유와 용어 해설: "뭘 하라는 건가?"]
- **비유**: 패스트푸드점 키오스크의 단계별 주문 화면
  - "1. 날짜를 찍으세요" ➔ "2. 수입인가요 지출인가요?" ➔ "3. 카테고리를 고르세요" 처럼 손님에게 하나씩 차근차근 물어보고, 잘못 적으면 친절하게 다시 물어보며 최종 주문 번호(ID) 영수증을 발행해 주라는 뜻입니다.

### 💻 [답안 구현 위치 및 코드]
- **구현 파일**: `budget_app/cli.py` & `budget_app/services.py`
```python
    if args.command == "add":
        tx_date = prompt_date()
        tx_type = prompt_type()
        tx_category = prompt_category(repo.load_categories())
        tx_amount = prompt_amount()
        tx_memo = prompt_memo()
        tx_tags = prompt_tags()

        new_id = service.create_transaction(
            date_str=tx_date, tx_type=tx_type, category=tx_category,
            amount=tx_amount, memo=tx_memo, tags=tx_tags
        )
        print(f"✔ 생성된 거래 ID: {new_id}")
```

---

## 6. 거래 목록 조회 (List) 및 정렬

### 📌 [과제 명세서의 문제 요구사항]
- `list`는 최신순으로 출력하고, `--limit N` 옵션을 지원할 것(기본값 10).
- 스트리밍 처리를 활용할 것.

### 💡 [3개월 차를 위한 쉬운 비유와 용어 해설: "뭘 하라는 건가?"]
- **비유**: 인스타그램 최신 피드 보기
  - 옛날 글이 먼저 나오면 불편하니, 가장 최근에 쓴 돈 내역이 맨 위에 오도록 날짜 역순으로 정렬해서 딱 10개만 깔끔한 표로 보여주라는 뜻입니다.

### 💻 [답안 구현 위치 및 코드]
- **구현 파일**: `budget_app/services.py`
```python
    def list_transactions(self, limit: int = 10) -> List[Transaction]:
        all_transactions = list(self.repo.stream_transactions())
        all_transactions.sort(key=lambda x: x.date, reverse=True)
        return all_transactions[:limit]
```

---

## 7. 거래 다중 조건 검색 (Search)

### 📌 [과제 명세서의 문제 요구사항]
- 기간(`--from`, `--to`), 카테고리(`--category`), 타입(`--type`), 메모 키워드(`--q`), 태그(`--tag`) 다중 조건 필터링.
- 최신순 정렬 및 스트리밍 처리 유지.

### 💡 [3개월 차를 위한 쉬운 비유와 용어 해설: "뭘 하라는 건가?"]
- **비유**: 쇼핑몰 상세 필터 검색
  - "지난달 1일부터 말일까지 중에서, '식비' 카테고리이고, 메모에 '치킨'이 들어간 지출만 쏙쏙 골라줘!"라는 다중 조건을 체로 거르듯 골라내라는 뜻입니다.

### 💻 [답안 구현 위치 및 코드]
- **구현 파일**: `budget_app/services.py`
```python
    def filter_transactions_stream(
        self, from_date: Optional[str] = None, to_date: Optional[str] = None,
        category: Optional[str] = None, tx_type: Optional[str] = None,
        keyword: Optional[str] = None, tag: Optional[str] = None,
    ) -> Generator[Transaction, None, None]:
        for tx in self.repo.stream_transactions():
            if from_date and tx.date < from_date: continue
            if to_date and tx.date > to_date: continue
            if category and tx.category != category: continue
            if tx_type and tx.type != tx_type: continue
            if keyword and (keyword not in tx.memo): continue
            if tag and (tag not in tx.tags): continue
            yield tx
```

---

## 8. 거래 수정(Update) 및 삭제(Delete)

### 📌 [과제 명세서의 문제 요구사항]
- `delete --id <id>`, `update --id <id> [필드별 옵션]` 지원.
- 없는 ID 입력 시 "없는 데이터" 안내 메시지 출력.
- 파일 재작성 시 원자적 교체로 안정성을 보장할 것.

### 💡 [3개월 차를 위한 쉬운 비유와 용어 해설: "뭘 하라는 건가?"]
- **비유**: 장부의 특정 줄만 지우개로 지우거나 빨간 펜으로 고치기
  - 거래 번호(ID)를 찾아서 그 영수증만 쏙 빼고 버리거나(삭제), 날짜나 금액만 싹 고친 뒤(수정) 다시 장부를 묶어두는 작업입니다.

### 💻 [답안 구현 위치 및 코드]
- **구현 파일**: `budget_app/services.py`
```python
    def delete_transaction(self, tx_id: str) -> bool:
        all_tx = list(self.repo.stream_transactions())
        remaining_tx = [tx for tx in all_tx if tx.id != tx_id]
        if len(all_tx) == len(remaining_tx):
            return False
        self.repo.overwrite_transactions(remaining_tx)
        return True
```

---

## 9. 카테고리 관리 (Category) 및 데이터 보호

### 📌 [과제 명세서의 문제 요구사항]
- `category add/list/remove` 기능 제공.
- **필수 보호 조건**: 이미 거래 내역에서 사용 중인 카테고리는 삭제를 막거나 방어할 것.

### 💡 [3개월 차를 위한 쉬운 비유와 용어 해설: "뭘 하라는 건가?"]
- **비유**: 부모 없는 자식(고아 데이터) 방지
  - 내가 '식비'로 100만 원 쓴 내역이 장부에 수십 개 남아있는데, 카테고리 목록에서 '식비'를 덜컥 삭제해 버리면 나중에 가계부 통계를 낼 때 분류 불가능 상태가 됩니다.
  - 그래서 "이 카테고리를 쓴 영수증이 1개라도 남아있으면 삭제 절대 불가!"라고 문을 걸어 잠그라는 뜻입니다.

### 💻 [답안 구현 위치 및 코드]
- **구현 파일**: `budget_app/services.py`
```python
    def remove_category(self, name: str) -> None:
        clean_name = name.strip()
        cats = self.repo.load_categories()
        if clean_name not in cats:
            raise ValueError(f"존재하지 않는 카테고리입니다: '{clean_name}'")

        for tx in self.repo.stream_transactions():
            if tx.category == clean_name:
                raise ValueError(
                    f"카테고리 '{clean_name}'을(를) 사용 중인 거래 내역(ID: {tx.id} 등)이 존재합니다.\n"
                    f"관련 거래를 먼저 수정하거나 삭제한 후에 카테고리를 삭제할 수 있습니다."
                )

        cats.remove(clean_name)
        self.repo.save_categories(cats)
```

---

## 10. 월별 요약(Summary) 및 예산 경고(Budget)

### 📌 [과제 명세서의 문제 요구사항]
- `budget set --month YYYY-MM --amount <금액>` 예산 영구 저장.
- `summary --month YYYY-MM [--top N]` 실행 시:
  - 총수입, 총지출, 잔액(수입-지출) 출력.
  - 카테고리별 지출 금액 합계 상위 TOP N 출력.
  - 예산 대비 사용률(%) 및 예산 초과 시 경고 문구 출력.
  - 데이터가 없는 달은 "데이터 없음" 명확히 안내.

### 💡 [3개월 차를 위한 쉬운 비유와 용어 해설: "뭘 하라는 건가?"]
- **비유**: 한 달 가계부 성적표와 과소비 경고 사이렌
  - 이번 달 총 얼마 벌었고 얼마 썼는지 계산하고, "내가 이번 달에 어디에 돈을 제일 많이 썼지? 1등 식비, 2등 쇼핑..." 순위를 매겨줍니다.
  - 만약 이번 달 목표 예산이 50만 원이었는데 60만 원을 썼다면 삐뽀삐뽀 빨간 경고등(예산 초과 알림)을 울려주라는 뜻입니다.

### 💻 [답안 구현 위치 및 코드]
- **구현 파일**: `budget_app/services.py` & `budget_app/cli.py`
```python
# [services.py]
    def get_monthly_summary(self, month: str, top_n: int = 3) -> Optional[Dict[str, Any]]:
        total_income, total_expense = 0, 0
        category_expenses = defaultdict(int)
        has_data = False

        for tx in self.repo.stream_transactions():
            if tx.date.startswith(month):
                has_data = True
                if tx.type == "income": total_income += tx.amount
                elif tx.type == "expense":
                    total_expense += tx.amount
                    category_expenses[tx.category] += tx.amount

        if not has_data: return None

        sorted_top = sorted(category_expenses.items(), key=lambda x: x[1], reverse=True)[:top_n]
        return {
            "month": month, "income": total_income, "expense": total_expense,
            "balance": total_income - total_expense, "top_categories": sorted_top,
            "budget": self.repo.get_budget(month),
        }
```

---

## 11. CSV 가져오기(Import) 및 내보내기(Export)

### 📌 [과제 명세서의 문제 요구사항]
- `export --out <csv> [--month | --from & --to]`로 CSV 파일 생성.
- `import --from <csv>`로 외부 거래 내역 일괄 등록.
- 필수 CSV 고정 스키마(`date`, `type`, `category`, `amount`, `memo`, `tags`), UTF-8 헤더 포함.

### 💡 [3개월 차를 위한 쉬운 비유와 용어 해설: "뭘 하라는 건가?"]
- **비유**: 엑셀 파일로 서류 뽑아주기 & 엑셀 서류 받아먹기
  - 회사 상사나 세무사에게 가계부를 넘겨줄 때 엑셀(CSV)로 예쁘게 뽑아서 전달하고, 반대로 카드사에서 다운로드받은 엑셀 내역을 내 가계부 프로그램에 밥 먹이듯 꿀꺽 흡수시키라는 뜻입니다.

### 💻 [답안 구현 위치 및 코드]
- **구현 파일**: `budget_app/services.py`
```python
    def export_transactions_csv(self, out_path: str, month: Optional[str] = None,
                                from_date: Optional[str] = None, to_date: Optional[str] = None) -> int:
        if month:
            from_date, to_date = f"{month}-01", f"{month}-31"
        if not from_date or not to_date:
            raise ValueError("내보내기 조건을 지정해야 합니다.")

        exported_count = 0
        with open(out_path, "w", encoding="utf-8", newline="") as f:
            writer = csv.writer(f)
            writer.writerow(["date", "type", "category", "amount", "memo", "tags"])
            for tx in self.repo.stream_transactions():
                if from_date <= tx.date <= to_date:
                    writer.writerow([tx.date, tx.type, tx.category, tx.amount, tx.memo, ",".join(tx.tags)])
                    exported_count += 1
        return exported_count
```

---

## 12. 공통 관심사 데코레이터 및 예외 제어(Decorator & Exit Code)

### 📌 [과제 명세서의 문제 요구사항]
- 공통 관심사(예외 처리, 로그, 실행 시간 측정) 데코레이터를 구현하고 적용할 것.
- 오류 발생 시 흉측한 스택트레이스(Traceback) 노출 금지 ➔ **원인 + 해결 힌트** 출력.
- 정상 종료 시 exit code 0, 예외 종료 시 0이 아닌 값(`sys.exit(1)`)으로 종료할 것.

### 💡 [3개월 차를 위한 쉬운 비유와 용어 해설: "뭘 하라는 건가?"]
- **비유**: 마법 방패 옷과 친절한 안내원
  - 프로그램에서 에러가 났을 때 영어로 된 무시무시한 컴퓨터 속살(Traceback)을 화면에 쏟아내면 손님이 공포에 질립니다.
  - 함수 머리 위에 `@handle_cli_errors`라는 방패 옷을 입혀두면, 에러가 나는 순간 방패가 가로채서 **"원인: 날짜 형식이 틀렸습니다 / 힌트: YYYY-MM-DD로 적으세요"**라고 친절히 안내한 뒤 안전하게 문을 닫고 나가라는 뜻입니다.

### 💻 [답안 구현 위치 및 코드]
- **구현 파일**: `budget_app/decorators.py`
```python
def handle_cli_errors(func: F) -> F:
    @functools.wraps(func)
    def wrapper(*args: Any, **kwargs: Any) -> Any:
        try:
            return func(*args, **kwargs)
        except ValueError as ve:
            print(f"\n[오류 원인] 잘못된 값 또는 형식이 전달되었습니다: {ve}", file=sys.stderr)
            print("[해결 힌트] 날짜(YYYY-MM-DD), 양수 금액, 허용된 카테고리인지 다시 확인하세요.", file=sys.stderr)
            sys.exit(1)
        except Exception as e:
            print(f"\n[시스템 오류] 예기치 않은 오류가 발생했습니다: {e}", file=sys.stderr)
            print("[해결 힌트] 인자 표기(--) 및 명령어 형식이 명세와 일치하는지 점검하세요.", file=sys.stderr)
            sys.exit(1)
    return wrapper
```
