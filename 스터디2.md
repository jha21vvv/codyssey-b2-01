# 📘 [코디세이 b2-01] 콘솔 가계부 애플리케이션 요구사항 & 완벽 구현 스터디 가이드 (스터디2)

---

## 1. 미션 소개

> **[원본 명세서 내용]**  
> '작은 서비스'란 기능이 많은 게 아니라 예외 상황에서도 데이터가 안전한 것을 말합니다. 이번에 만드는 건 콘솔 가계부인데, 제너레이터 스트리밍, 데코레이터 분리, 타입 힌트까지 구조를 제대로 챙깁니다. 단순한 프로그램이 아니라, 유지보수 가능한 설계로 완성한다는 관점으로 접근하세요.  
> 이번 미션은 Python으로 파일 입출력 기반의 가계부 콘솔 프로그램을 구현하는 과제입니다. 수입과 지출 내역을 단순히 저장하는 수준을 넘어, 수정/삭제, 검색, 월별 요약, 카테고리 관리, 예산 초과 경고까지 포함한 "작은 서비스" 형태로 완성합니다.  
> 학습자는 터미널에서 명령어를 입력해 내역을 추가하고(add), 목록을 조회하며(list), 특정 조건으로 검색하고(search), 월별 요약과 카테고리 리포트를 출력하고(summary), 데이터를 내보내고(export), 기존 파일을 가져오는(import) 기능을 구현합니다. 데이터는 프로그램 종료 후에도 유지되도록 JSONL 또는 CSV 파일로 영구 저장해야 합니다.  
> 또한, 저장 파일을 한 번에 모두 읽지 않고 제너레이터로 스트리밍 처리하며, 데코레이터로 공통 기능(예외 처리, 실행 로그, 실행 시간 측정 등)을 분리합니다. 타입 힌트를 적용해 함수와 데이터 구조의 계약을 명확히 하고, 모듈 분리로 유지보수 가능한 구조를 설계합니다.

---

### 💡 [3개월 차 개발자를 위한 1차 설명 & 전문용어 0% 2차 설명]
- **[ 마이 설명 ]**
 - 제너레이터(Generator): 데이터를 한 번에 다 만들어두지 않고, 필요할 때마다 하나씩 생성(Generate)해 내는 특수한 함수
 - stream_transactions(): 장부 파일 열기 ──▶ 한 줄 읽기 ──▶ 영수증 객체로 부활 ──▶ yield로 회계사에게 1장 배달 ──▶ 다음 줄 읽기 (반복)
 - 타입힌트: 코드에 "이 변수는 무슨 타입이어야 하고, 결과물은 무슨 타입으로 나간다"고 명시적으로 이름표를 달아두는 문법 (예시:def add(a: int, b: int) -> int)
- **[1차 설명: 코드가 실제로 하는 일]**
  - 터미널에서 명령어로 돈을 기록하고, 엑셀/텍스트 파일로 영구 저장하며, 실수로 엉뚱한 값을 넣어도 프로그램이 꺼지지 않고 친절히 안내하도록 기능을 분리하여 만듭니다.
- **[2차 업무/비유 설명: 실제 실생활 업무 관점]**
  - **"이면지에 낙서하지 말고, 영구 보존되는 철제 장부 캐비닛을 만들자!"**
  - 초보 때는 모든 코드를 한 파일에 몰아넣고 print와 input만 마구 씁니다. 하지만 실제 회사에서는 컴퓨터가 꺼지거나 정전이 나도 손님의 소중한 돈 거래 내역이 절대 찢어지거나 사라지지 않는 **[안전한 금고 시스템]**을 만들어야 합니다.
  - 카운터 안내원(`cli.py`), 회계사(`services.py`), 지하 창고지기(`repositories.py`), 공식 전표 양식(`models.py`), 안전 에어백(`decorators.py`)으로 각자의 방을 나누어 일하게 만든 것입니다.

---

### 💻 [요구사항에 맞게 구현된 코드 및 1차/2차 설명]
- **구현 파일**: `budget_app/__init__.py`, `budget_app/__main__.py`

```python
# [파일 이름]: budget_app/__init__.py
# [1차 설명]: 현재 가계부 프로그램의 공식 배포 버전을 적어둔 변수입니다.
# [2차 업무 설명]: "이 폴더는 잡동사니 폴더가 아니라 진짜 가계부 정품 상자구나!" 하고 파이썬이 공식 인정하게 만들고, 버전 이름표 도장을 찍어둡니다.
__version__ = "1.0.0"
```

```python
# [파일 이름]: budget_app/__main__.py
# [1차 설명]: cli.py 파일 안에서 메뉴 화면을 켜주는 run_cli 함수를 가져옵니다.
# [2차 업무 설명]: 매장 문을 열었을 때 손님을 맞이할 카운터 안내원(run_cli)을 카운터 자리로 불러오는 일입니다.
from .cli import run_cli

# [1차 설명]: 이 파일이 터미널에서 사용자가 직접 켠 파일인지 확인하는 조건문입니다.
# [2차 업무 설명]: "손님이 진짜로 매장 문을 열고 들어와서 시동을 건 것이 맞는가?"를 확인하는 안전 점검입니다.
if __name__ == "__main__":
    # [1차 설명]: 카운터 메뉴 화면을 실제로 띄우는 run_cli() 함수를 실행합니다.
    # [2차 업무 설명]: 카운터 안내원이 손님에게 메뉴판을 펼치고 "어서 오세요!" 하고 주문을 받기 시작합니다.
    run_cli()
```

---

## 2. 최종 결과물 (10가지 핵심 기능)

> **[원본 명세서 내용]**  
> 다음 10가지 기능이 정상 동작하는 애플리케이션 1개를 완성한다.  
> 1. 거래 추가(add): 대화형 입력(날짜, 타입, 카테고리, 금액, 메모/태그) ➔ 저장 성공 메시지 + 생성된 거래 id  
> 2. 거래 목록(list): --limit 등 옵션 ➔ 최신순 거래 리스트(스트리밍 처리)  
> 3. 거래 검색(search): --from/--to, --category, --type, --q, --tag ➔ 조건에 맞는 거래 리스트(최신순)  
> 4. 월별 요약(summary): --month YYYY-MM, --top N ➔ 총수입/총지출/잔액 + 카테고리별 지출 TOP N  
> 5. 예산 설정/조회(budget): budget set --month YYYY-MM --amount 금액 ➔ 저장 성공 메시지 + summary에서 예산 사용률/초과 경고  
> 6. 카테고리 관리(category): category add/list/remove ➔ 카테고리 목록/추가/삭제 결과(사용 중 카테고리 처리 포함)  
> 7. 거래 수정(update): --id 기반 ➔ 수정 성공/실패 메시지(없는 id 처리 포함)  
> 8. 거래 삭제(delete): delete --id <id> ➔ 삭제 성공/실패 메시지(없는 id 처리 포함)  
> 9. 가져오기/내보내기(import/export): import --from <csv>, export --out <csv> --month ... 또는 --from/--to ➔ 처리 건수 출력 + CSV 파일 생성/반영 확인  
> 10. 추가 조건(필수 구성): 데이터는 3개 이상 파일(transactions, categories, budgets)로 영구 저장되어야 하며, README.md에 가이드가 포함되어야 한다.

---

### 💡 [3개월 차 개발자를 위한 1차 설명 & 전문용어 0% 2차 설명]
- **[1차 설명: 코드가 실제로 하는 일]**
  - 손님이 터미널에서 칠 수 있는 10가지 세부 메뉴(추가, 조회, 검색, 요약, 예산, 카테고리, 수정, 삭제, 내보내기, 가져오기)를 파이썬의 `argparse` 서브명령어로 구현하고, 실제 데이터 처리는 회계사(`BudgetService`)에게 위임합니다.
- **[2차 업무/비유 설명: 실제 실생활 업무 관점]**
  - **"10가지 버튼이 달린 최신형 키오스크 접수대"**
  - 손님이 키오스크 화면에서 '거래 추가' 버튼을 누르면 질문지가 하나씩 나오고, '검색' 버튼을 누르면 돋보기로 장부를 뒤져주며, '삭제'를 누르면 해당 영수증 번호표를 파기하는 10가지 업무 창구를 완성한 것입니다.

---

### 💻 [요구사항에 맞게 구현된 코드 및 1차/2차 설명]
- **구현 파일**: `budget_app/cli.py` (명령어 파서 등록 및 메뉴 분기)

```python
# [파일 이름]: budget_app/cli.py (233행~345행)
    # [1차 설명]: 1번째 메뉴인 'add'(거래 추가) 명령어를 등록합니다.
    # [2차 업무 설명]: 손님이 터미널에 'add'라고 치면 대화형으로 질문을 던져 거래를 추가하도록 메뉴판에 등록합니다.
    subparsers.add_parser("add", help="대화형으로 새로운 거래 내역을 추가합니다.")

    # [1차 설명]: 2번째 메뉴인 'list'(목록 조회) 명령어를 등록합니다.
    # [2차 업무 설명]: 최신 거래 목록을 보여주는 전용 메뉴판을 만듭니다.
    list_parser = subparsers.add_parser("list", help="최신 거래 내역 목록을 조회합니다.")
    # [1차 설명]: 몇 개까지 출력할지 지정하는 --limit 옵션을 추가합니다.
    # [2차 업무 설명]: 안 적으면 기본 10개만 보여주겠다는 옵션 구멍을 뚫어둡니다.
    list_parser.add_argument("--limit", type=int, default=10, help="출력할 최대 거래 개수 (기본값: 10)")

    # [1차 설명]: 3번째 메뉴인 'search'(검색) 명령어를 등록합니다.
    # [2차 업무 설명]: 기간, 카테고리, 메모, 태그로 영수증을 찾는 전용 메뉴판입니다.
    search_parser = subparsers.add_parser("search", help="다양한 조건으로 거래 내역을 검색합니다.")
    search_parser.add_argument("--from", dest="from_date", help="검색 시작일 (YYYY-MM-DD)")
    search_parser.add_argument("--to", dest="to_date", help="검색 종료일 (YYYY-MM-DD)")
    search_parser.add_argument("--category", help="카테고리 일치 검색")
    search_parser.add_argument("--type", choices=["income", "expense"], help="거래 타입")
    search_parser.add_argument("--q", dest="keyword", help="메모 검색 키워드")
    search_parser.add_argument("--tag", help="특정 태그 포함 여부")

    # [1차 설명]: 4번째 메뉴인 'delete'(삭제) 명령어를 등록합니다.
    # [2차 업무 설명]: 번호표(ID)를 지정해 영수증을 파기하는 메뉴판입니다.
    del_parser = subparsers.add_parser("delete", help="거래 내역을 삭제합니다.")
    del_parser.add_argument("--id", required=True, help="삭제할 거래 ID")

    # [1차 설명]: 5번째 메뉴인 'update'(수정) 명령어를 등록합니다.
    # [2차 업무 설명]: 번호표(ID)를 지정해 날짜나 금액을 고쳐 적는 메뉴판입니다.
    update_parser = subparsers.add_parser("update", help="거래 내역을 수정합니다.")
    update_parser.add_argument("--id", required=True, help="수정할 대상 거래 ID")
    update_parser.add_argument("--date", help="새로운 날짜 (YYYY-MM-DD)")
    update_parser.add_argument("--type", choices=["income", "expense"], help="새로운 거래 타입")
    update_parser.add_argument("--category", help="새로운 카테고리")
    update_parser.add_argument("--amount", type=int, help="새로운 금액 (양수 정수)")
    update_parser.add_argument("--memo", help="새로운 메모")
    update_parser.add_argument("--tags", help="새로운 태그 (쉼표로 구분)")

    # [1차 설명]: 6번째 메뉴인 'category'(카테고리 관리) 명령어를 등록합니다.
    # [2차 업무 설명]: 목록(list), 추가(add), 삭제(remove)를 묶어서 관리합니다.
    cat_parser = subparsers.add_parser("category", help="카테고리를 관리합니다.")
    cat_sub = cat_parser.add_subparsers(dest="category_action", help="카테고리 작업 선택")
    cat_sub.add_parser("list", help="전체 카테고리 목록을 확인합니다.")
    cat_add = cat_sub.add_parser("add", help="새로운 카테고리를 추가합니다.")
    cat_add.add_argument("name", help="추가할 카테고리 이름")
    cat_del = cat_sub.add_parser("remove", help="카테고리를 삭제합니다.")
    cat_del.add_argument("name", help="삭제할 카테고리 이름")

    # [1차 설명]: 7번째 메뉴인 'budget'(예산 관리) 명령어를 등록합니다.
    # [2차 업무 설명]: 월별 목표 예산 카드를 등록하는 메뉴판입니다.
    budget_parser = subparsers.add_parser("budget", help="월별 예산을 관리합니다.")
    budget_sub = budget_parser.add_subparsers(dest="budget_action", help="예산 작업 선택")
    b_set = budget_sub.add_parser("set", help="월별 예산을 설정합니다.")
    b_set.add_argument("--month", required=True, help="설정 대상 월 (YYYY-MM)")
    b_set.add_argument("--amount", type=int, required=True, help="예산 금액 (양수 정수)")

    # [1차 설명]: 8번째 메뉴인 'summary'(월별 요약 리포트) 명령어를 등록합니다.
    # [2차 업무 설명]: 한 달 번 돈, 쓴 돈, 지출 1~3위 순위표를 계산해 주는 메뉴판입니다.
    sum_parser = subparsers.add_parser("summary", help="월별 요약 리포트를 출력합니다.")
    sum_parser.add_argument("--month", required=True, help="조회할 대상 월 (YYYY-MM)")
    sum_parser.add_argument("--top", type=int, default=3, help="지출 상위 카테고리 수 (기본값: 3)")

    # [1차 설명]: 9번째 메뉴인 'export'(CSV 내보내기) 명령어를 등록합니다.
    # [2차 업무 설명]: 거래들을 엑셀 파일로 인쇄해 주는 메뉴판입니다.
    exp_parser = subparsers.add_parser("export", help="조건에 맞는 거래 내역을 CSV 파일로 내보냅니다.")
    exp_parser.add_argument("--out", required=True, help="저장할 CSV 파일 경로 (필수)")
    exp_parser.add_argument("--month", help="내보낼 대상 월 (YYYY-MM)")
    exp_parser.add_argument("--from", dest="from_date", help="시작일 (YYYY-MM-DD)")
    exp_parser.add_argument("--to", dest="to_date", help="종료일 (YYYY-MM-DD)")

    # [1차 설명]: 10번째 메뉴인 'import'(CSV 가져오기) 명령어를 등록합니다.
    # [2차 업무 설명]: 외부 엑셀 파일을 읽어 내 장부로 일괄 등록하는 메뉴판입니다.
    imp_parser = subparsers.add_parser("import", help="CSV 파일로부터 거래 내역을 일괄 가져옵니다.")
    imp_parser.add_argument("--from", dest="input_file", required=True, help="가져올 CSV 파일 경로 (필수)")
```

---

## 3. 과제 목표

> **[원본 명세서 내용]**  
> 이 과제를 마친 후, 학습자는 아래를 스스로 설명할 수 있어야 한다.  
> * 파일 기반 저장(JSONL/CSV)으로 데이터를 영구 저장하고, CRUD/검색/요약/입출력을 구현할 수 있다.  
> * 콘솔 프로그램을 클래스/모듈로 구조화하고, 각 계층(모델/저장소/서비스/CLI)의 책임을 설명할 수 있다.  
> * yield 기반 제너레이터로 대용량 파일도 스트리밍 처리하는 이유와 동작 방식을 설명할 수 있다.  
> * 데코레이터로 공통 관심사(로그/예외/시간 측정) 를 분리한 구조와 이유를 설명할 수 있다.  
> * 타입 힌트를 통해 입출력 계약을 명확히 했을 때 얻는 이점을 실제 코드 예로 설명할 수 있다.

---

### 💡 [3개월 차 개발자를 위한 1차 설명 & 전문용어 0% 2차 설명]
## 마이 설명
- CRUD: C (Create - 생성): 새로운 데이터를 만들어 파일이나 DB에 저장하는 동작, R (Read - 조회): 저장되어 있는 데이터를 불러와서 읽거나 검색하는 동작, U (Update - 수정): 이미 존재하는 데이터를 찾아서 내용을 변경하는 동작,D (Delete - 삭제): 필요 없어진 데이터를 지우는 동작
- yield 기반 제너레이터로 대용량 파일도 스트리밍 처리하는 이: 처리가 끝난 1장은 즉시 램에서 지우고(쓰레기 수거, 가비지 컬렉션), 그다음 2번째 장 딱 1장만 램에 새로 올립니다.
- **[1차 설명: 코드가 실제로 하는 일]**
  - 데이터를 RAM 칠판에만 두지 않고 하드디스크 파일에 적어 보존하며, 10만 줄짜리 큰 파일도 컴퓨터가 멈추지 않게 1줄씩 꺼내는 `yield` 문법과 공통 에러 처리를 데코레이터로 분리했습니다.
- **[2차 업무/비유 설명: 실제 실생활 업무 관점]**
  - **"10만 장짜리 백과사전을 한꺼번에 안고 오다 허리 삐끗하지 말자!"**
  - 10만 장의 영수증을 한꺼번에 컴퓨터 메모리에 올리면 컴퓨터가 버벅거리거나 튕깁니다. `yield`는 마치 **"회전초밥 레일"**처럼 손님이 먹을 접시를 하나씩만 꺼내 올려주는 기술입니다.
  - 서비스 파일에서 for tx in stream_transactions() 방식: 정수기 수도꼭지입니다. 물을 컵으로 1잔 받아서 마신 뒤, 컵을 비우고 다음 1잔을 또 받아서 마시는 것입니다. 10만 잔의 물을 마시더라도 내 손에는 언제나 컵 1개분의 무게만 들려 있습니다

---

### 💻 [요구사항에 맞게 구현된 코드 및 1차/2차 설명]
- **구현 파일**: `budget_app/repositories.py` (스트리밍 제너레이터)

```python
# [파일 이름]: budget_app/repositories.py (97행~110행)
    # [1차 설명]: 장부 파일에서 영수증을 한 장씩 꺼내서 호출자에게 전달해 주는 함수입니다.
    # [2차 업무 설명]: 회전초밥 레일처럼, 10만 장짜리 거대한 장부에서 영수증을 딱 한 장씩만 꺼내서 회계사 손에 쥐여주는 일입니다.
    def stream_transactions(self) -> Generator[Transaction, None, None]:
        # [1차 설명]: 거래 장부 파일이 존재하는지 검사합니다.
        if not os.path.exists(self.tx_file):
            return

        # [1차 설명]: 장부 파일을 읽기 모드(r)로 열어 파일 관리자를 준비합니다.
        with open(self.tx_file, "r", encoding="utf-8") as f:
            # [1차 설명]: 파일에서 한 줄씩 순서대로 읽습니다.
            for line in f:
                # [1차 설명]: 줄 앞뒤의 불필요한 줄바꿈과 공백을 잘라냅니다.
                line_str = line.strip()
                if not line_str:
                    continue
                # [1차 설명]: 파일에 적힌 한 줄 글자를 딕셔너리로 바꿉니다.
                data = json.loads(line_str)
                # [1차 설명]: 정식 Transaction 영수증으로 조립해서 밖으로 1장 배달합니다.
                # [2차 업무 설명]: yield는 회전초밥 접시에 영수증 1장을 얹어서 회계사 손에 건네주는 배달 명령입니다.
                yield Transaction.from_dict(data)
```

---

## 4. 기능 요구 사항 (세부 항목별)

### 4.1 실행 및 입력 방식

> **[원본 명세서 내용]**  
> * 실행 예시는 아래 중 하나를 권장합니다: python -m budget_app <command> [options]  
> * 모든 명령은 --help 옵션으로 사용 방법이 출력되어야 합니다.  
> * 입력 기본 방식은 “대화형” 입니다 (예: add 실행 시 날짜/타입/카테고리/금액 등을 input()으로 순차 입력).  
> * 단, search, list, summary, export, import, delete는 옵션 인자 방식 허용(권장).  
> * update는 “옵션 방식 또는 대화형” 중 하나를 선택(문서 고정).  
> * 옵션 표기는 리눅스 표준인 --로 통일.

---

### 💡 [3개월 차 개발자를 위한 1차 설명 & 전문용어 0% 2차 설명]
- **[1차 설명: 코드가 실제로 하는 일]**
  - `add`는 질문을 던져 키보드로 하나씩 답을 받는 대화형 `input()`으로 구현하고, 나머지 복잡한 검색이나 수정/삭제는 `--id`, `--month` 같은 `--` 옵션 방식으로 깔끔하게 입력받습니다.
- **[2차 업무 설명: 실제 실생활 업무 관점]**
  - 매장에 처음 온 손님이 거래를 적을 때는 카운터 직원이 친절하게 "언제 쓰셨어요?", "얼마 쓰셨어요?" 하고 하나씩 질문을 던져주고(`대화형`), 이미 잘 아는 관리자가 내역을 검색하거나 지울 때는 빠른 옵션 스위치를 쓰는 방식입니다.

---

### 💻 [요구사항에 맞게 구현된 코드 및 1차/2차 설명]
- **구현 파일**: `budget_app/cli.py` (대화형 질문 도우미 함수)

```python
# [파일 이름]: budget_app/cli.py (72행~94행)
# [1차 설명]: 올바른 날짜를 입력받을 때까지 계속 질문하는 함수입니다.
# [2차 업무 설명]: 손님에게 날짜를 묻고 오타가 나면 다시 묻는 창구입니다.
def prompt_date() -> str:
    # [1차 설명]: 올바른 날짜가 입력되어 return을 만날 때까지 영원히 반복합니다.
    while True:
        # [1차 설명]: 키보드로 날짜 글자를 입력받고 양쪽의 불필요한 공백을 잘라냅니다.
        # [2차 업무 설명]: input()으로 질문을 띄우고 엔터를 기다리며 .strip()으로 빈칸을 지웁니다.
        val = input("1. 날짜 (YYYY-MM-DD): ").strip()
        # [1차 설명]: 입력한 날짜가 진짜 달력에 존재하는지 검사를 시도합니다.
        try:
            # [1차 설명]: 문자열을 "년-월-일" 달력 날짜 형식으로 변환해 봅니다.
            datetime.strptime(val, "%Y-%m-%d")
            # [1차 설명]: 올바른 날짜이면 입력받은 글자를 호출자에게 돌려주고 루프를 끝냅니다.
            return val
        # [1차 설명]: 달력에 없는 엉터리 날짜이거나 글자 모양이 틀렸을 때 발생하는 에러를 잡습니다.
        except ValueError:
            # [1차 설명]: 형식 안내 오류 메시지를 콘솔 화면에 보여줍니다.
            print("   [입력 오류] 'YYYY-MM-DD' 형식(예: 2026-05-15)으로 정확히 입력하세요.")
```

---

### 4.2 데이터 모델 (Data Model)

> **[원본 명세서 내용]**  
> * 거래 내역(Transaction)은 최소 id(유일), type(income/expense), date(YYYY-MM-DD), amount(양수), category, memo(선택), tags(선택) 필드를 포함해야 합니다.  
> * 데이터 모델은 dataclass 또는 그에 준하는 구조로 정의해야 합니다.  
> * 최소 2개 이상의 클래스를 사용해야 합니다 (예: Transaction, Budget, Category 등).  
> * 입력 검증: 날짜 형식 오류, 음수/0 금액, 허용되지 않은 type, 존재하지 않는 category 등은 오류 메시지 출력/예외 처리.

---

### 💡 [3개월 차 개발자를 위한 1차 설명 & 전문용어 0% 2차 설명]
- **[1차 설명: 코드가 실제로 하는 일]**
  - 파이썬 `@dataclass`를 사용하여 `Transaction`(영수증), `Budget`(예산 카드), `Category`(카테고리 명찰) 3개의 규격 클래스를 만들고, `__post_init__` 검사관을 두어 엉터리 데이터가 들어오면 즉시 거절합니다.
- **[2차 업무 설명: 실제 실생활 업무 관점]**
  - **"은행의 공식 입출금 전표 양식"**
  - 이면지에 대충 적은 낙서는 거절하고, 반드시 공식 전표 양식에 적어야만 장부에 등록해 줍니다. 전표를 내자마자 은행원이 돋보기를 들고 마이너스 금액이나 없는 날짜를 1초 만에 잡아내는 것과 같습니다.

---

### 💻 [요구사항에 맞게 구현된 코드 및 1차/2차 설명]
- **구현 파일**: `budget_app/models.py` (영수증 데이터 모델 및 자체 검사관)

```python
# [파일 이름]: budget_app/models.py (54행~110행)
# [1차 설명]: 거래 내역 1건을 보관하는 영수증 클래스입니다.
# [2차 업무 설명]: 돈을 벌거나 쓸 때마다 작성하는 '가계부 공식 영수증 종이'입니다.
@dataclass
class Transaction:
    id: str                                  # [1차 설명]: 고유 번호표 / [2차 설명]: 영수증 일련번호
    type: Literal["income", "expense"]       # [1차 설명]: 수입/지출 구분 / [2차 설명]: 들어온 돈인가 나간 돈인가
    date: str                                # [1차 설명]: 거래 날짜 문자열 / [2차 설명]: YYYY-MM-DD 날짜 도장
    amount: int                              # [1차 설명]: 거래 금액 양의 정수 / [2차 설명]: 원 단위 돈 액수
    category: str                            # [1차 설명]: 카테고리 이름 / [2차 설명]: 식비, 교통 등 사용처
    memo: str = ""                           # [1차 설명]: 추가 메모 / [2차 설명]: 김치찌개 등 덧붙인 메모
    tags: list[str] = field(default_factory=list) # [1차 설명]: 태그 리스트 / [2차 설명]: 외식, 점심 등 꼬리표

    # [1차 설명]: 영수증이 만들어지자마자 내용물이 진짜 올바른지 검사하는 함수입니다.
    # [2차 업무 설명]: 영수증을 창구에 내자마자 은행원이 돋보기를 들고 가짜 날짜나 마이너스 돈을 잡아내는 일입니다.
    def __post_init__(self) -> None:
        # [1차 설명]: 거래 구분이 수입이나 지출 둘 중 하나가 맞는지 확인합니다.
        if self.type not in ("income", "expense"):
            raise ValueError(f"유효하지 않은 거래 타입입니다: '{self.type}'. 'income' 또는 'expense'여야 합니다.")

        # [1차 설명]: 날짜 문자열이 진짜 달력에 존재하는 날인지 검사를 시도합니다.
        try:
            datetime.strptime(self.date, "%Y-%m-%d")
        except ValueError:
            raise ValueError(f"유효하지 않은 날짜 형식입니다: '{self.date}'. 'YYYY-MM-DD' 형식이어야 합니다.")

        # [1차 설명]: 금액이 숫자가 아니거나 0원 이하(마이너스)인지 확인합니다.
        if not isinstance(self.amount, int) or self.amount <= 0:
            raise ValueError(f"금액은 0보다 큰 양수 정수여야 합니다: {self.amount}")

        # [1차 설명]: 카테고리 칸이 비어있거나 공백 스페이스바만 있는지 확인합니다.
        if not self.category or not self.category.strip():
            raise ValueError("카테고리는 비어 있을 수 없습니다.")
```

---

### 4.3 저장 정책 (Storage Policy & Atomic Write)

> **[원본 명세서 내용]**  
> * 저장 포맷은 JSONL 또는 CSV 중 1개를 선택합니다 (JSONL 선택).  
> * 저장 파일은 3개 이상(필수) 로 분리해 영구 저장합니다 (transactions.jsonl, categories.jsonl, budgets.jsonl).  
> * 기본 저장 폴더는 ./data를 권장하며, 옵션으로 변경 가능해야 합니다(--data-dir).  
> * 초기 실행 시 파일이 없으면 자동 생성하거나 초기화해야 합니다.  
> * 카테고리 파일이 비어있으면 (안 A) 기본 카테고리 자동 생성(식비, 교통, 주거, 쇼핑, 급여, 기타).

---

### 💡 [3개월 차 개발자를 위한 1차 설명 & 전문용어 0% 2차 설명]
- **[1차 설명: 코드가 실제로 하는 일]**
  - `./data` 폴더 아래에 3개의 파일(`transactions.jsonl`, `categories.jsonl`, `budgets.jsonl`)을 생성하고, 파일이 없을 때 기본 카테고리 6개를 자동으로 채워 넣습니다.
  - 수정이나 삭제를 할 때는 `tempfile`과 `os.replace`를 써서 도중에 컴퓨터가 꺼져도 파일이 깨지지 않게 안전하게 저장합니다.
- **[2차 업무 설명: 실제 실생활 업무 관점]**
  - **"비밀 연습장 바꿔치기 마술"**
  - 장부를 고치다가 갑자기 정전이 나면 장부 책이 찢어져서 다 날아갑니다. 그래서 창고지기는 **연습장에 먼저 새 내용을 완벽하게 적어둔 뒤, 0.001초 만에 원본 장부와 싹 맞바꿔치기(`os.replace`)**합니다. 정전이 나도 원본 장부는 100% 안전합니다.

---

### 💻 [요구사항에 맞게 구현된 코드 및 1차/2차 설명]
- **구현 파일**: `budget_app/repositories.py` (초기화 및 원자적 파일 교체)

```python
# [파일 이름]: budget_app/repositories.py (79행~90행 & 120행~135행)
    # [1차 설명]: 빈 장부 파일을 준비하고 기본 카테고리를 채워 넣는 초기화 함수입니다.
    # [2차 업무 설명]: 새 가계부를 처음 쓰는 손님을 위해 빈 공책을 꺼내두고 기본 분류(식비, 교통 등)를 미리 적어두는 일입니다.
    def _initialize_storage(self) -> None:
        # [1차 설명]: 거래 장부와 예산 장부가 없으면 깨끗한 빈 파일을 만듭니다.
        for path in (self.tx_file, self.budget_file):
            if not os.path.exists(path):
                open(path, "a", encoding="utf-8").close()

        # [1차 설명]: 카테고리 장부가 비어있다면 기본 카테고리 6개를 채웁니다.
        # [2차 업무 설명]: 처음 온 손님이 바로 가계부를 쓸 수 있게 "식비", "교통", "주거", "쇼핑", "급여", "기타"를 미리 적어둡니다.
        if not os.path.exists(self.cat_file) or os.path.getsize(self.cat_file) == 0:
            default_categories = ["식비", "교통", "주거", "쇼핑", "급여", "기타"]
            self.save_categories(default_categories)

    # [1차 설명]: 기존 거래 목록 전체를 안전한 임시 파일 방식으로 덮어씁니다.
    # [2차 업무 설명]: 연습장에 먼저 완벽히 베껴 쓴 다음 0.001초 만에 원본 장부와 싹 맞바꿔치기하는 일입니다.
    def overwrite_transactions(self, transactions: List[Transaction]) -> None:
        dir_name = os.path.dirname(self.tx_file)
        # [1차 설명]: 임시 연습장 파일을 만듭니다.
        with tempfile.NamedTemporaryFile("w", dir=dir_name, delete=False, encoding="utf-8") as tf:
            temp_path = tf.name
            for tx in transactions:
                line = json.dumps(tx.to_dict(), ensure_ascii=False)
                tf.write(line + "\n")
            tf.flush()
            # [1차 설명]: 하드디스크에 물리적으로 완전히 기록되도록 강제합니다.
            # [2차 업무 설명]: 종이에 잉크가 번지지 않도록 도장을 꾹 눌러서 완전히 말려 굳히는 일입니다.
            os.fsync(tf.fileno())

        # [1차 설명]: 임시 파일을 원본 파일로 순간 교체합니다.
        # [2차 업무 설명]: 연습장을 원본 장부 자리에 순식간에 쏙 집어넣어 교체를 끝냅니다.
        os.replace(temp_path, self.tx_file)
```

---

### 4.4 add (거래 추가) & list (목록 조회)

> **[원본 명세서 내용]**  
> * add: 대화형으로 필드를 입력받아 거래를 저장. category는 등록된 목록에 존재해야 함. 저장 완료 시 생성된 id를 출력.  
> * list: 최신순으로 거래를 출력. --limit N 옵션 지원(기본값 제공). 파일 전체를 한 번에 로드하지 않고 제너레이터 기반 스트리밍 처리.

---

### 💡 [3개월 차 개발자를 위한 1차 설명 & 전문용어 0% 2차 설명]
- **[1차 설명: 코드가 실제로 하는 일]**
  - `add`: 6단계 대화형 질문을 거친 뒤 8자리 고유 ID(`uuid.uuid4()[:8]`)를 발급하여 장부에 저장하고 손님 화면에 번호표를 띄웁니다.
  - `list`: 창고지기에게서 받은 거래들을 최신 날짜 순으로 정렬한 뒤, 손님이 요청한 개수(`--limit`)만큼 보기 좋은 표로 출력합니다.
- **[2차 업무 설명: 실제 실생활 업무 관점]**
  - **"번호표 발급기 & 최신순 정렬 표"**
  - 매장에서 주문을 넣으면 접수증에 8자리 영수증 번호(`a1b2c3d4`)를 도장 찍어 손님에게 건네주고, 장부를 펼칠 때는 오늘 쓴 돈이 가장 맨 위에 오도록 표를 만들어 보여주는 일입니다.

---

### 💻 [요구사항에 맞게 구현된 코드 및 1차/2차 설명]
- **구현 파일**: `budget_app/services.py` (거래 생성 및 리스트 정렬)

```python
# [파일 이름]: budget_app/services.py (109행~125행 & 158행~161행)
        # [1차 설명]: 세상에 겹치지 않는 8자리 고유 번호(ID)를 발급합니다.
        # [2차 업무 설명]: 영수증마다 고유 주민번호처럼 8자리 번호표(예: a1b2c3d4)를 딱 찍어줍니다.
        unique_id = str(uuid.uuid4())[:8]

        # [1차 설명]: 검사관을 통과한 공식 영수증 객체를 조립합니다.
        tx = Transaction(
            id=unique_id,
            type=tx_type,  # type: ignore[arg-type]
            date=date_str,
            amount=amount,
            category=category,
            memo=memo,
            tags=tags,
        )
        # [1차 설명]: 창고지기에게 장부 맨 끝에 적어두라고 건넵니다.
        self.repo.append_transaction(tx)
        # [1차 설명]: 발급된 8자리 번호표를 반환합니다.
        return unique_id

    # [1차 설명]: 최근 거래 목록을 최신 날짜 순으로 limit 개수만큼 정렬하여 가져옵니다.
    # [2차 업무 설명]: 가장 최근에 쓴 돈 내역이 맨 위에 오도록 영수증을 날짜 역순으로 줄 세워 딱 10장만 보여주는 업무입니다.
    def list_transactions(self, limit: int = 10) -> List[Transaction]:
        all_transactions = list(self.repo.stream_transactions())
        all_transactions.sort(key=lambda x: x.date, reverse=True)
        return all_transactions[:limit]
```

---

### 4.5 update / delete (수정 / 삭제)

> **[원본 명세서 내용]**  
> * delete --id <id>로 특정 거래를 삭제할 수 있어야 합니다. 존재하지 않는 id는 “없는 데이터”로 처리.  
> * update는 옵션 기반(update --id <id> [--date ...] [--amount ...] 등)으로 구현.  
> * 임시 파일/원자적 교체 등 파일 안정성을 고려.

---

### 💡 [3개월 차 개발자를 위한 1차 설명 & 전문용어 0% 2차 설명]
- **[1차 설명: 코드가 실제로 하는 일]**
  - 전달받은 `--id`를 장부에서 찾아내어 수정 필드만 덮어쓰거나 해당 줄만 삭제하며, 없는 ID가 들어오면 `False`를 돌려주어 안내 문구를 띄웁니다.
- **[2차 업무 설명: 실제 실생활 업무 관점]**
  - **"장부 줄 긋고 고쳐 쓰기 & 영수증 파기"**
  - 손님이 "아까 1234번 영수증 만 원이 아니라 2만 원인데요?" 하면 장부에서 1234번 줄을 찾아 빨간 펜으로 금액을 고쳐 적고, "영수증 취소해 주세요!" 하면 그 영수증만 쏙 빼서 분쇄기에 넣는 업무입니다.

---

### 💻 [요구사항에 맞게 구현된 코드 및 1차/2차 설명]
- **구현 파일**: `budget_app/services.py` (삭제 및 수정 로직)

```python
# [파일 이름]: budget_app/services.py (190행~214행)
    # [1차 설명]: 거래 번호(ID)를 찾아 해당 영수증을 장부에서 파기합니다.
    # [2차 업무 설명]: 손님이 "1234번 영수증 찢어버려 주세요!" 했을 때 장부에서 그 영수증만 쏙 빼서 버리는 업무입니다.
    def delete_transaction(self, tx_id: str) -> bool:
        all_tx = list(self.repo.stream_transactions())
        remaining_tx = [tx for tx in all_tx if tx.id != tx_id]
        # [1차 설명]: 삭제 전 개수와 남은 개수가 같다면 해당 ID가 장부에 없었다는 뜻입니다.
        if len(all_tx) == len(remaining_tx):
            return False
        # [1차 설명]: 살아남은 영수증들로 장부를 안전하게 다시 덮어씁니다.
        self.repo.overwrite_transactions(remaining_tx)
        return True

    # [1차 설명]: 거래 번호(ID)의 거래 내용을 새로운 내용으로 고쳐 씁니다.
    def update_transaction(self, tx_id: str, ...) -> bool:
        all_tx = list(self.repo.stream_transactions())
        target_tx = next((tx for tx in all_tx if tx.id == tx_id), None)
        # [1차 설명]: 대상 영수증을 찾지 못했는지 검사합니다.
        if target_tx is None:
            return False
        # ... (각 필드 검증 및 갱신 후 repo.overwrite_transactions 호출)
```

---

### 4.6 search (검색) & summary (월별 요약) & budget (예산 경고)

> **[원본 명세서 내용]**  
> * search: 기간(--from/--to), 카테고리(--category), 타입(--type), 메모 키워드(--q), 태그(--tag) 다중 필터 지원. 최신순 출력.  
> * summary: --month YYYY-MM 지원. 총수입, 총지출, 잔액(총수입-총지출), 지출 상위 TOP N 카테고리 출력. 데이터가 없는 달은 “데이터 없음” 출력.  
> * budget: budget set --month YYYY-MM --amount <금액> 저장. summary 실행 시 예산 사용률(%) 및 초과 시 경고 문구 출력.

---

### 💡 [3개월 차 개발자를 위한 1차 설명 & 전문용어 0% 2차 설명]
- **[1차 설명: 코드가 실제로 하는 일]**
  - `search`: 6가지 조건을 만족하는 영수증만 체로 걸러내듯 골라냅니다.
  - `summary`: 해당 월의 영수증을 모아 수입 합계, 지출 합계, 잔액을 계산하고 지출 1~3위 순위를 매깁니다.
  - `budget`: 설정된 예산 한도와 실제 쓴 돈을 비교하여 `(쓴 돈 / 예산) * 100` 퍼센트를 구하고, 예산을 넘겼으면 삐뽀삐뽀 빨간 사이렌 경고를 울립니다.
- **[2차 업무 설명: 실제 실생활 업무 관점]**
  - **"월말 결산 성적표와 과소비 경고 사이렌"**
  - 한 달이 끝나면 수석 총무가 계산기를 두드려 "이번 달에는 식비로 제일 많이 썼네요!" 하고 1위부터 3위까지 랭킹을 매겨주고, "목표 예산 50만 원인데 60만 원 쓰셨어요! 10만 원 초과 과소비입니다!" 하고 경고 도장을 쾅 찍어주는 업무입니다.

---

### 💻 [요구사항에 맞게 구현된 코드 및 1차/2차 설명]
- **구현 파일**: `budget_app/services.py` (월별 요약 및 예산 결산 로직)

```python
# [파일 이름]: budget_app/services.py (307행~350행)
    # [1차 설명]: 해당 월의 수입, 지출, 잔액, 지출 상위 카테고리 TOP N을 계산하여 결산 리포트를 만듭니다.
    # [2차 업무 설명]: 한 달이 끝나면 계산기를 두드려 수입/지출/잔액 성적표와 지출 랭킹을 만드는 업무입니다.
    def get_monthly_summary(self, month: str, top_n: int = 3) -> Optional[Dict[str, Any]]:
        # ... (형식 검증 및 변수 초기화)
        total_income = 0
        total_expense = 0
        category_expenses: Dict[str, int] = defaultdict(int)
        has_data = False

        # [1차 설명]: 장부의 영수증들을 하나씩 순회하며 계산합니다.
        for tx in self.repo.stream_transactions():
            if tx.date.startswith(month):
                has_data = True
                if tx.type == "income":
                    total_income += tx.amount
                elif tx.type == "expense":
                    total_expense += tx.amount
                    category_expenses[tx.category] += tx.amount

        if not has_data:
            return None

        # [1차 설명]: 돈을 가장 많이 쓴 순서대로 카테고리를 정렬하여 1위부터 TOP N위를 자릅니다.
        sorted_top_categories = sorted(category_expenses.items(), key=lambda item: item[1], reverse=True)[:top_n]
        budget = self.repo.get_budget(month)

        return {
            "month": month,
            "income": total_income,
            "expense": total_expense,
            "balance": total_income - total_expense,
            "top_categories": sorted_top_categories,
            "budget": budget,
        }
```

```python
# [파일 이름]: budget_app/cli.py (665행~686행: 예산 초과 사이렌 출력부)
        # [1차 설명]: 실제로 쓴 돈이 목표 예산을 초과했는지 비교합니다.
        # [2차 설명]: 과소비 사이렌을 울릴 조건문입니다.
        if summary["expense"] > b_amount:
            # [1차 설명]: 예산을 얼마 초과했는지 초과액을 뺄셈 계산합니다.
            over_amount = summary["expense"] - b_amount
            # [1차 설명]: 빨간 경고 사이렌 문구를 콘솔에 출력합니다.
            print(f"\n※ [경고] 설정한 월 예산을 초과했습니다! (초과액: {over_amount:,}원)")
        else:
            remaining_budget = b_amount - summary["expense"]
            print(f"• 남은 예산 : {remaining_budget:>12,}원")
```

---

### 4.7 category (카테고리 보호)

> **[원본 명세서 내용]**  
> * category add/list/remove를 제공해야 합니다.  
> * 카테고리 삭제 시, 해당 카테고리를 사용하는 내역이 존재하면 삭제를 막아야 합니다.

---

### 💡 [3개월 차 개발자를 위한 1차 설명 & 전문용어 0% 2차 설명]
- **[1차 설명: 코드가 실제로 하는 일]**
  - 카테고리를 지우려고 할 때 장부(`transactions.jsonl`) 전체를 수색하여 그 카테고리를 쓴 영수증이 단 1장이라도 있으면 에러를 던져 삭제를 원천 차단합니다.
- **[2차 업무 설명: 실제 실생활 업무 관점]**
  - **"고아 영수증 방지 안전 자물쇠"**
  - 장부에 '식비'로 쓴 영수증이 100장이나 남아있는데 '식비'라는 분류 이름표를 지워버리면 그 100장의 영수증은 어디에 썼는지 알 수 없는 미아(고아)가 됩니다. 그래서 관련 영수증이 장부에 남아있으면 이름표를 절대 못 버리게 손목을 딱 잡는 일입니다.

---

### 💻 [요구사항에 맞게 구현된 코드 및 1차/2차 설명]
- **구현 파일**: `budget_app/services.py` (카테고리 사용 여부 전수 조사)

```python
# [파일 이름]: budget_app/services.py (278행~285행)
        # [1차 설명]: 장부를 뒤져서 이 카테고리를 쓴 영수증이 있는지 전수 조사합니다.
        # [2차 업무 설명]: 창고지기에게 영수증을 1장씩 받으며 이 이름표를 쓴 영수증이 있는지 수색합니다.
        for tx in self.repo.stream_transactions():
            # [1차 설명]: 영수증의 카테고리가 삭제하려는 이름과 같은지 확인합니다.
            if tx.category == clean_name:
                # [1차 설명]: 사용 중인 카테고리 삭제 불가 에러를 던집니다.
                # [2차 업무 설명]: 영수증이 장부에 남아있으니 먼저 거래를 지우거나 수정하라고 안내합니다.
                raise ValueError(
                    f"카테고리 '{clean_name}'을(를) 사용 중인 거래 내역(ID: {tx.id} 등)이 존재합니다.\n"
                    f"관련 거래를 먼저 수정하거나 삭제한 후에 카테고리를 삭제할 수 있습니다."
                )
```

---

### 4.8 import / export (CSV 입출력)

> **[원본 명세서 내용]**  
> * import --from <csv>로 거래를 일괄 등록한다.  
> * export --out <csv>로 조건에 맞는 거래를 CSV로 저장한다 (--month 또는 --from/--to 필수).  
> * CSV 최소 스키마 고정: date(YYYY-MM-DD), type(income/expense), category(등록 카테고리), amount(양수 정수), memo(선택), tags(선택).

---

### 💡 [3개월 차 개발자를 위한 1차 설명 & 전문용어 0% 2차 설명]
- **[1차 설명: 코드가 실제로 하는 일]**
  - 파이썬 기본 `csv` 모듈을 써서 가계부 장부 데이터를 엑셀용 CSV 파일로 인쇄하거나, 외부 CSV의 행을 한 줄씩 읽어 거래 유효성 검사를 거쳐 일괄 등록합니다.
- **[2차 업무 설명: 실제 실생활 업무 관점]**
  - **"엑셀 서류 인쇄기 & 카드사 내역 일괄 흡수기"**
  - 가계부 내역을 엑셀로 깔끔하게 인쇄해서 상사에게 보고하거나, 은행/카드사에서 다운로드받은 엑셀 파일을 장부로 한 번에 쓱 집어넣어 자동으로 등록하는 업무입니다.

---

### 💻 [요구사항에 맞게 구현된 코드 및 1차/2차 설명]
- **구현 파일**: `budget_app/services.py` (CSV 내보내기/가져오기)

```python
# [파일 이름]: budget_app/services.py (376행~386행 & 420행~431행)
        # [1차 설명]: 출력할 CSV 파일을 쓰기 모드로 엽니다.
        with open(out_path, "w", encoding="utf-8", newline="") as f:
            writer = csv.writer(f)
            writer.writerow(["date", "type", "category", "amount", "memo", "tags"])
            for tx in self.repo.stream_transactions():
                if from_date <= tx.date <= to_date:
                    tags_str = ",".join(tx.tags) if tx.tags else ""
                    writer.writerow([tx.date, tx.type, tx.category, tx.amount, tx.memo, tags_str])
                    exported_count += 1
        return exported_count

        # ... (import 함수 내부)
                # [1차 설명]: 회계사의 거래 생성 함수를 호출하여 장부에 등록을 시도합니다.
                try:
                    self.create_transaction(
                        date_str=date_val,
                        tx_type=type_val,
                        category=cat_val,
                        amount=amount_val,
                        memo=memo_val,
                        tags=tags_list,
                    )
                    imported_count += 1
                except ValueError as ve:
                    raise ValueError(f"[행 {row_idx} 데이터 오류] {ve}")
```

---

## 5. 공통 비기능 요구사항 (데코레이터, 예외 처리, 모듈화)

> **[원본 명세서 내용]**  
> 1. 데코레이터: 공통 관심사(예외 처리/로그/시간 측정) 데코레이터를 1개 이상 구현하고 실제 적용한다.  
> 2. 예외 처리 및 종료 코드: 오류는 스택트레이스 대신 원인 + 해결 힌트로 출력한다. 정상 종료는 0, 오류 종료는 0이 아닌 값(1)으로 종료한다.  
> 3. 모듈화: 한 파일에 몰아넣지 않고 최소 3개 이상 모듈로 분리한다 (CLI / 서비스 / 저장소 / 모델).

---

### 💡 [3개월 차 개발자를 위한 1차 설명 & 전문용어 0% 2차 설명]
- **[1차 설명: 코드가 실제로 하는 일]**
  - `@handle_cli_errors` 데코레이터를 만들어 메인 함수 `run_cli()`를 감싸두었습니다. 도중에 오타나 파일 오류가 나도 무서운 영어 Traceback 대신 깔끔한 `[오류 원인]`과 `[해결 힌트]`를 출력하고 `sys.exit(1)`로 안전하게 프로그램을 마칩니다.
- **[2차 업무 설명: 실제 실생활 업무 관점]**
  - **"서커스 공연장 바닥의 푹신한 에어백 그물망"**
  - 곡예사가 공중그네를 타다가 손을 놓쳤을 때 맨바닥에 떨어지면 관객들이 피를 보고 비명을 지릅니다(빨간 영어 에러 도배). 바닥에 푹신한 에어백 매트리스를 깔아두면 다치지 않고 튕겨 올라오며, 안내원이 "관객 여러분, 잠시 줄을 다시 정비하겠습니다" 하고 친절히 방송하는 것과 같습니다.

---

### 💻 [요구사항에 맞게 구현된 코드 및 1차/2차 설명]
- **구현 파일**: `budget_app/decorators.py` (에어백 예외 처리 데코레이터)

```python
# [파일 이름]: budget_app/decorators.py (66행~122행)
# [1차 설명]: 모든 콘솔 명령어의 에러를 가로채서 친절히 안내해 주는 데코레이터 함수입니다.
# [2차 업무 설명]: 명령어 실행 시 바닥에 푹신하게 깔아두는 [안전 에어백 매트리스]입니다.
def handle_cli_errors(func: F) -> F:
    @functools.wraps(func)
    def wrapper(*args: Any, **kwargs: Any) -> Any:
        try:
            # [1차 설명]: 원래 함수를 정상 실행해 봅니다.
            return func(*args, **kwargs)
        # [1차 설명]: 값 오류가 터졌을 때 에어백이 낚아챕니다.
        except ValueError as ve:
            # [1차 설명]: 스택트레이스 대신 친절한 원인과 힌트를 출력합니다.
            print(f"\n[오류 원인] 잘못된 값 또는 형식이 전달되었습니다: {ve}", file=sys.stderr)
            print("[해결 힌트] 날짜(YYYY-MM-DD), 양수 금액, 허용된 카테고리인지 다시 확인하세요.", file=sys.stderr)
            # [1차 설명]: 오류 종료 코드 1로 프로그램을 안전하게 마칩니다.
            sys.exit(1)
        # ... (KeyError, FileNotFoundError, PermissionError, KeyboardInterrupt 처리)
    return wrapper
```

---

## 6. 개발 환경 및 제약 사항

> **[원본 명세서 내용]**  
> * 개발 환경: Python 3.10 이상  
> * 라이브러리 제약: 표준 라이브러리만 사용 가능 (외부 pip install 금지).  
> * 저장 방식: JSONL 선택, 3개 이상 파일 분리.  
> * CLI 규칙: 옵션 표기는 --로 통일.  
> * 오류 처리: 스택트레이스 출력 금지, 오류 종료 시 exit code != 0.  
> * 저장소: https://github.com/jha21vvv/codyssey-b2-01

---

### 💡 [3개월 차 개발자를 위한 1차 설명 & 전문용어 0% 2차 설명]
- **[1차 설명: 코드가 실제로 하는 일]**
  - 오직 파이썬 3.10 기본 도구(`json`, `csv`, `os`, `tempfile`, `dataclasses`, `argparse`, `functools`, `uuid`, `datetime`)만 사용하여 외부 설치 없이 어디서든 즉시 실행되도록 제작했습니다.
- **[2차 업무 설명: 실제 실생활 업무 관점]**
  - 다른 사람 컴퓨터에 가서 프로그램을 돌릴 때 인터넷에서 이것저것 다운로드받을 필요 없이, 파이썬만 깔려 있으면 USB로 옮겨서 바로 1초 만에 실행할 수 있는 순수 무결점 프로그램입니다.

---

### 💻 [요구사항에 맞게 구현된 코드 및 1차/2차 설명]
- **구현 파일**: `budget_app/cli.py` & 전체 패키지 의존성

```python
# [파일 이름]: budget_app/cli.py (39행~60행)
# [1차 설명]: 순수 파이썬 표준 라이브러리만 임포트합니다.
# [2차 업무 설명]: pip 설치가 필요 없는 파이썬 기본 제공 만능 공구함에서 도구를 꺼냅니다.
import argparse                      # 명령어 번역기
import sys                           # 시스템 및 종료 코드 제어기
from datetime import datetime        # 달력 책
from typing import List              # 타입 명찰 도구
from .decorators import handle_cli_errors  # 에어백 그물망
from .repositories import FileRepository   # 창고지기
from .services import BudgetService        # 수석 회계사
```
